import { pb } from "oicq/lib/core";
import { GuildApp } from "./app";
import { Channel } from "./channel";
export declare enum GuildRole {
    Member = 1,
    GuildAdmin = 2,
    Owner = 4,
    ChannelAdmin = 5
}
export interface GuildMember {
    tiny_id: string;
    card: string;
    nickname: string;
    role: GuildRole;
    join_time: number;
}
export declare class Guild {
    readonly app: GuildApp;
    readonly guild_id: string;
    guild_name: string;
    channels: Map<string, Channel>;
    constructor(app: GuildApp, guild_id: string);
    _renew(guild_name: string, proto: pb.Proto | pb.Proto[]): void;
    /** 获取频道成员列表 */
    getMemberList(): Promise<GuildMember[]>;
}
