import { MessageElem } from "oicq";
import { pb } from "oicq/lib/core";
import { Sendable } from "oicq/lib/message";
export declare class GuildMessage {
    /** 频道id */
    guild_id: string;
    guild_name: string;
    /** 子频道id */
    channel_id: string;
    channel_name: string;
    /** 消息序号(同一子频道中一般顺序递增) */
    seq: number;
    rand: number;
    time: number;
    message: MessageElem[];
    raw_message: string;
    sender: {
        tiny_id: string;
        nickname: string;
    };
    constructor(proto: pb.Proto);
    /** 暂时仅支持发送： 文本、AT、表情 */
    reply(content: Sendable): Promise<{
        seq: number;
        rand: number;
        time: number;
    }>;
}
